<div class="p-comment__item">
  <p><%= simple_format(comment.comment)%></p>
  <div class="p-comment__bottomLine">
    <span><%= coment.name%></span>
    <span><%= coment.created_at.to_s(datetime_jp)%></span>
    <span><%= link_to '削除', coment, method: :delete, data: { confirm: '削除してもよろしいいですか？'}%></span>
  </div>
</div>
