<div class="d-flex align-item-center mt-4 md-4">
  <div class="ml-auto boards__linkBox">
    <%= link_to '掲示板一覧',boards_path ,class: 'btn btn-outline-dark' %>
    <%= link_to '編集画面', edit_board_path(@board) ,class: 'btn btn-outline-dark' %>

  </div>
</div>
<% if flash[:notice]%>
  <div class="alert alert-primary"><%= flash[:notice]%></div>
<% end %>
<%= render @board %>
<div class="p-comment__list">
  <div class="p-comment_listTitle">コメント</div>
  <%= render @board.comments%>
</div>
<%= render partial: 'comments/form', locals: {comment: @comment } %>
